# $Id: docker-compose.yml,v 1.1 2023/02/14 07:49:03 bibi Exp bibi $

version: '3'

services:

  vindecoder-server:
    build: .
#!!    restart: unless-stopped
    volumes:
#FIXME: incorrect after git project creation
       - ./httpd.conf:/usr/local/apache2/conf/httpd.conf:ro
       - ./my-cgi.sh:/usr/local/apache2/cgi-bin/my-cgi.cgi:ro
    ports:
       - "9999:80"
    environment:
       - SETUP=${SETUP:-"false"}
